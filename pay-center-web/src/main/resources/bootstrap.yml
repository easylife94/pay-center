spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: pay-config-server
      profile: pay-center
eureka:
  instance:
    hostname: pay-center
  client:
    service-url:
      defaultZone: http://pay-eureka-server:8761/eureka
